float *f25706486(float *data, int data_length) {
  for (int i = 0; i < data_length; i++) {
    data[i] = (data[i]*2);
  }

  return data;
}
float *f58378985(float *data, int data_length) {
  for (int i = 0; i < data_length; i++) {
    data[i] = (data[i]*2);
  }

  return data;
}
float *f68741746(float *data, int data_length) {
  for (int i = 0; i < data_length; i++) {
    data[i] = (data[i]*2);
  }

  return data;
}
float *f27301695(float *data, int data_length) {
  for (int i = 0; i < data_length; i++) {
    data[i] = (data[i]*2);
  }

  return data;
}
float *f22101960(float *data, int data_length) {
  for (int i = 0; i < data_length; i++) {
    data[i] = (((7*data[i])*2)+data[i]);
  }

  return data;
}
